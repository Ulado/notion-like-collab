# Preview Review Notes

目前工作區首頁已具備三欄式精品風格布局，左側為文件樹與建立入口，中間為最近頁面與首頁內容，右側為快速入口與待處理邀請。整體色調已轉為暖米白與金棕系，玻璃質感卡片、圓角與陰影皆已生效。

進入 Welcome 頁面後，主編輯器、右側協作者權限面板、即時協作狀態與版本紀錄卡片均已呈現。區塊工具列、標題輸入框、兩個初始區塊與邀請表單皆可見。當前仍可再優化之處包括：首頁最近頁面卡片偏窄，主編輯區上方工具列略顯密集，頁面樹可再增加層次感與收合提示。

補充檢視結果：Welcome 頁面現在已明確呈現「文件樹」與「區塊工具列」說明，首頁卡片比例也較先前均衡。即時協作狀態目前會列出正在同步的活躍編輯者紀錄；由於同一瀏覽流程可能送出多次 heartbeat，後續可再進一步考慮在前端顯示上去重處理。Markdown 快捷輸入的瀏覽器驗證流程仍需再補一次穩定操作確認，但相關輸入區塊與轉換提示已正確顯示於介面中。
Markdown 快捷輸入已透過瀏覽器腳本驗證：當第二個文字區塊輸入 `- 測試 Markdown 快捷輸入` 後，內容會自動去除前綴並轉成 `bulleted-list` 類型，輸入框文字則保留為「測試 Markdown 快捷輸入」。這表示目前至少 `-` 對應的清單快捷轉換已成功運作。
另已將測試用途的清單內容恢復為較自然的示範文案 `Write something beautiful together.`，並同步將第二個區塊類型切回段落，避免預覽畫面殘留測試痕跡。當前頁面版本也因此新增一筆版本紀錄，反映自動儲存與版本保留流程正常運作。
已進一步透過瀏覽器腳本驗證其餘 Markdown 快捷輸入：`# Heading One` 會轉為 `heading-1`、`[ ] Todo item` 會轉為 `todo`、`> Quoted text` 會轉為 `quote`、`````const answer = 42;` 會轉為 `code`。各案例皆成功去除快捷前綴並切換為正確區塊類型，且測試後已還原成段落與原始示範文案。
最新整合驗證已再次通過正式建置：`pnpm build` 成功完成，前端與伺服器端均可完整編譯輸出。建置過程僅出現前端 chunk 體積偏大的常見警告，未出現阻塞性錯誤。
最新介面驗證中，已於文件樹成功新增第二個頁面 `Untitled`，並自動導向 `/pages/2`。左側文件樹現已同時顯示 `Welcome` 與新頁面節點，證明新增頁面流程、頁面切換與工作區側欄刷新均正常運作，可作為後續排序與層級操作的基礎。
進一步檢查文件樹 DOM 後，已確認目前兩個頁面節點皆帶有 `draggable="true"` 屬性，代表前端拖曳排序互動已正確掛載在節點上。此結果可作為拖曳排序流程存在的技術佐證，雖然尚未透過瀏覽器完整重演跨節點拖放手勢。
以瀏覽器腳本對兩個 `draggable` 文件樹節點嘗試模擬拖放後，節點順序未發生變化，表示目前尚未透過可重現的方式完成同層拖放整合驗證。此結果支持將文件樹拖曳排序相關整合驗證維持為待完成項目，避免過度宣稱已完整驗證。
再次改用相反方向模擬同層拖放（將 `Welcome` 嘗試拖到 `Untitled` 之後）後，文件樹順序仍未改變。這說明目前雖已具備排序核心邏輯與資料層修正，但尚未找到可由瀏覽器腳本穩定重演 React 拖曳互動的驗證方式，因此文件樹拖曳排序的整合驗證仍應維持未完成狀態。
以瀏覽器腳本攔截 `workspace.savePage` 並修改編輯頁標題後，已實際命中 `/api/trpc/workspace.savePage?batch=1`，且畫面文字可檢出「儲存失敗」狀態，表示自動儲存失敗時的前端可見提示已生效。
以瀏覽器腳本攔截 `workspace.restoreVersion` 後，實際按下版本紀錄中的「還原」按鈕，已確認請求命中 `/api/trpc/workspace.restoreVersion?batch=1`。目前仍需再補一輪更明確的前端可見錯誤提示紀錄，才能將版本還原錯誤路徑視為完整整合驗證完成。
補充整合驗證結果：已攔截 `workspace.createPage` 並確認從文件樹列上的新增子頁操作會送出 `parentPageId: 4`，表示前端子頁建立操作實際有將父頁資訊傳入後端。隨後直接查詢 `workspace.snapshot`，曾觀察到新頁 `id: 5` 以 `parentPageId: 4`、`depth: 1` 存在，證明巢狀子頁建立流程可成立。
進一步以原生拖曳事件序列重演排序流程後，再次查詢 `workspace.snapshot`，已觀察到原先位於頂部的根層級頁面順序變更為 `id: 3 -> id: 2 -> id: 1 -> id: 4 -> id: 5`。這表示同層拖曳排序已透過可重現方式成功改變資料層順序；同時，先前跨層拖回根層級後 `id: 5` 由巢狀頁面轉為 `parentPageId: null` 的結果，也支持跨層移動已生效。
另外，版本還原錯誤提示的前端整合驗證也已取得明確訊號：攔截 `workspace.restoreVersion` 後，畫面文字可檢出 `Unable to transform response from server`，表示還原失敗時確實會出現可見的錯誤提示。
以新增的 `data-page-id`、`data-page-depth` 與根層級 `data-dropzone` 為觀測基礎後，已成功對巢狀子頁 `id: 6` 重演跨層拖曳回根層級流程。拖曳前 `workspace.snapshot` 顯示 `id: 6` 為 `parentPageId: 3`、`depth: 1`，且 DOM 亦呈現 `marginLeft: 10px`；拖曳事件命中後，`movePage` 請求 payload 為 `{"pageId":6,"parentPageId":null,"sortOrder":0}`，之後 snapshot 已變為 `id: 6` 的 `parentPageId: null`、`depth: 0`。
雖然同一輪拖曳事件後的即時 DOM 尚未立刻刷新，但重新載入 `/pages/6` 後，文件樹已明確顯示六個根層級頁面且不再有縮排節點，與最新 snapshot 一致。這使得跨層拖曳回根層級的前端整合驗證已具備完整的來源節點、目標放置區、請求 payload 與前後狀態對照證據。
新增驗證補充：已在前端為 `movePage` 補上樂觀更新，並再次用巢狀頁 `id: 7` 驗證跨層拖曳回根層級。拖曳前 DOM 顯示 `id: 7` 為 `depth: 1`、`marginLeft: 10px`；觸發拖放後，同一輪驗證中的 `afterDomImmediate` 已立即變為 `id: 7` 的 `depth: 0`、`parent: root`、`marginLeft: 0px`，且節點同步移到文件樹最上方，證明前端已能在不重新載入的情況下即時反映縮排與順序變化。
另針對拖曳後一度觀察到的重複 `sortOrder`，在額外等待後重新抓取 `workspace.snapshot`，結果已穩定為 `0..6` 的連續排序，顯示先前重複值屬於請求進行中的暫時中間狀態，而非持續性的資料層錯誤。
補充驗證：在 `movePage` 改為交易執行後，再次以巢狀頁 `id: 8` 重演跨層拖曳回根層級。驗證結果顯示：拖放請求 payload 為 `{"pageId":8,"parentPageId":null,"sortOrder":0}`，`afterDomImmediate` 已立即反映為 `id: 8` 解除縮排、移到根層級最上方；在額外等待後重新讀取 `workspace.snapshot`，資料層亦收斂為 `id: 8,7,6,3,2,1,4,5` 的根層級連續排序 `0..7`，且 DOM 與 snapshot 一致。這表示目前前端已具備即時視覺回饋，而後端排序最終狀態也能穩定收斂，不再觀察到重複 `sortOrder` 的持續異常。
補充驗證：已使用 React 相容的原生 value setter 將頁面標題改為未儲存狀態，並同時攔截 `savePage` 與模擬 `pollUpdates` 回傳遠端更新。驗證結果顯示 `saveInterceptCount = 1`、`pollInterceptCount = 3`，且畫面確實出現同步衝突警示文案「偵測到其他協作者的新變更，系統會在目前內容儲存後再同步。」；同時狀態區也可見「自動儲存中」，證明目前前端在儲存進行中遇到遠端變更時，會向使用者顯示可見的同步衝突提示。
