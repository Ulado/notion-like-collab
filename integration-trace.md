# End-to-End Verification Trace

本文件整理本輪協作文件平台的可追溯整合驗證證據，重點不在宣稱所有流程都由單一自動化腳本一次跑完，而是在於將**實際操作、對應觀測、驗證結果與佐證來源**串連起來，讓後續檢查、交付與回溯都有一致依據。

| 驗證主題 | 實際操作 | 可觀測結果 | 佐證來源 |
| --- | --- | --- | --- |
| 登入與身份狀態 | 以已登入狀態開啟工作區與頁面編輯視圖 | 右下角顯示目前使用者 Ravi Chen，頁面角色顯示為擁有者 | 頁面預覽、`workspace.snapshot`、既有權限路由測試 |
| 邀請接受安全性 | 修正邀請查詢與接受流程，將當前登入者 email 納入比對 | 非受邀者無法接受不屬於自己的邀請 | `server/workspace.router.test.ts`、`pnpm test` 通過 |
| 權限限制 | 以 router 測試覆蓋頁面操作、邀請與版本還原錯誤路徑 | 權限不足時會回傳對應錯誤，不會靜默放行 | `server/workspace.router.test.ts`、`pnpm test` 通過 |
| Markdown 快捷輸入 | 以瀏覽器腳本與測試檔驗證 `#`、`-`、`[]`、`>`、``` 對應轉換 | 區塊類型可依輸入規則轉換 | `server/markdown.shortcuts.test.ts`、瀏覽器驗證紀錄 |
| 自動儲存失敗提示 | 先前已攔截儲存流程並觸發編輯 | 前端可見儲存失敗狀態，不是僅在 console 出現錯誤 | `review-notes.md`、錯誤路徑驗證紀錄 |
| 同步衝突提示 | 以原生 setter 讓頁面進入未儲存狀態，攔截 `savePage` 並模擬 `pollUpdates` | 畫面出現「偵測到其他協作者的新變更，系統會在目前內容儲存後再同步。」 | `review-notes.md`、`exec_result_2026-04-20_22-54-34_533.txt` |
| 文件樹子頁建立 | 從頁面列的新增子頁操作建立巢狀頁 | `parentPageId` 正確寫入，頁面以 depth=1 出現在文件樹 | `workspace.snapshot`、`review-notes.md` |
| 同層拖放 | 透過可觀測 `data-*` 屬性與拖曳事件模擬重新排序 | movePage payload 送出，文件樹順序可改變 | 瀏覽器腳本輸出、`review-notes.md` |
| 跨層拖回根層級 | 將巢狀頁拖回 root dropzone | DOM 可即時解除縮排，稍後 snapshot 收斂為新的根層級順序 | `exec_result_2026-04-20_22-49-12_701.txt`、`exec_result_2026-04-20_22-49-37_396.txt`、`review-notes.md` |
| 排序資料穩定性 | 以交易包住 `movePage` 與重編號流程後再次驗證 | 最終 snapshot 不再保留重複 `sortOrder` 的持續異常 | `server/db.ts` 最新修正、後續 snapshot 驗證 |
| 型別、測試與建置 | 重新執行 `pnpm check`、`pnpm test`、`pnpm build` | 型別檢查、4 個測試檔共 15 個測試與正式建置皆通過 | 終端輸出紀錄 |

## 驗證說明

本輪最重要的風險點在於**拖曳排序是否只有 UI 假象**與**多人同步衝突是否有可見提示**。目前這兩項都已取得較完整的證據鏈。前者不只驗證 DOM 立即變化，還驗證了 `movePage` 請求 payload 與之後的 `workspace.snapshot` 最終資料狀態；後者則不只模擬遠端更新，也讓頁面實際進入未儲存與儲存中條件，確認畫面上的衝突文案確實會出現。

## 限制與備註

目前這份整合驗證仍屬於**可追溯的半自動驗證紀錄**，而非單一套件驅動的完整瀏覽器端到端測試。也就是說，現階段最強的保證來自於：Vitest 覆蓋資料與權限邏輯、瀏覽器腳本覆蓋高風險互動、建置與型別檢查覆蓋可交付性。若下一步要再提高交付信心，建議再補一套正式的前端端到端測試框架，用來固定驗證拖曳、邀請接受與多人同步流程。

## 關於前端拖曳驗證方式的界定

本專案目前對文件樹拖曳排序的前端驗證，採用的是**可觀測驗證腳本與紀錄**：包含 `data-*` 可觀測節點、瀏覽器腳本輸出、`workspace.snapshot` 比對，以及 `review-notes.md` / `integration-trace.md` 中的操作紀錄。這些內容足以證明互動流程、payload、DOM 與最終資料狀態之間的一致性。

這並不等同於已導入正式瀏覽器端到端測試框架。也就是說，當前交付重點是**可追溯、可重演、可檢查的驗證證據鏈**，而不是宣稱已完成獨立的瀏覽器自動化測試套件。
