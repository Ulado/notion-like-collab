# Project TODO

- [x] 以高質感、優雅且精緻的設計語言重塑整體介面主題、排版系統與互動細節
- [x] 建立以 Manus OAuth 為核心的登入驗證流程，並保留角色名稱為擁有者、編輯者、檢視者
- [x] 設計工作區、頁面、頁面關聯、協作者權限、邀請與版本紀錄的資料模型
- [x] 實作文件樹側邊欄，支援巢狀頁面、新增、刪除、重新命名與拖曳排序
- [x] 實作區塊式富文字編輯器，支援標題、段落、待辦清單、項目符號清單、數字清單、引用及程式碼區塊
- [x] 支援 Markdown 語法快捷輸入，包含 # 標題與 - 清單等輸入轉換
- [x] 建立頁面權限管理機制，允許擁有者邀請指定使用者並設定為編輯者或檢視者
- [x] 實作多人即時協作編輯能力，讓多位具有權限的使用者可同時編輯同一頁面並即時同步變更
- [x] 建立自動儲存流程與頁面歷史版本紀錄查閱功能
- [x] 建立工作區首頁 Dashboard，顯示最近編輯頁面與快速入口
- [x] 撰寫後端與前端關鍵流程的 Vitest 測試，驗證權限、頁面操作與編輯資料處理邏輯
- [x] 完成整合驗證，確認網站可獨立運行且不依賴使用者的 MCP 工具
- [x] 修正邀請查詢與接受邏輯，確保只有被邀請的指定使用者可看到並接受自己的邀請
- [x] 完善文件樹拖曳排序，加入同層重新編號與跨層移動後的穩定排序處理，並補測試
- [x] 補上文件樹排序核心邏輯與測試：驗證同層重新編號、跨層移動後排序穩定性，以及 movePage 資料層行為
- [x] 重新執行並記錄文件樹拖曳排序整合驗證，實際證明同層拖放與跨層拖回根層級都會改變文件樹順序或縮排
- [x] 完成並驗證 Markdown 快捷輸入的實際轉換行為（至少 #、-、[]、>、```）
- [x] 補齊並驗證其餘 Markdown 快捷輸入（至少 #、[]、>、```）的實際轉換行為，並加入對應測試
- [x] 補強協作同步顯示邏輯，避免同一使用者 heartbeat 造成重複顯示，並改善同步穩定性
- [x] 補強自動儲存失敗、同步衝突與版本還原等錯誤處理與測試
- [x] 補做一輪完整整合驗證紀錄，逐項覆蓋登入、邀請接受、權限限制、Markdown 快捷轉換、協作同步與拖曳排序
- [x] 補上版本還原失敗或權限不足時的錯誤處理與 Vitest 測試
- [x] 補齊同步衝突提示的前端可見整合驗證，並與已完成的自動儲存失敗、版本還原錯誤提示紀錄一併整理
- [x] 驗證文件樹的子頁建立流程，確認從頁面列上的新增子頁操作會真正建立巢狀頁面，且 `parentPageId` 會正確傳入後端
- [x] 在子頁建立後重新驗證跨層拖曳回根層級的整合流程，確認文件樹縮排與順序會實際改變
- [x] 補做可重現的跨層拖曳回根層級整合驗證：記錄來源節點、目標放置區、movePage 請求 payload，以及變更前後 snapshot 與 DOM 縮排差異
- [x] 為文件樹拖曳排序新增更可觀測的前端驗證方式，覆蓋同層拖放與跨層拖回根層級流程
- [x] 補強跨層拖曳回根層級後的前端即時更新，確保 drop 後不需重新載入即可在 DOM 反映縮排與順序變化
- [x] 新增更穩定的前端觀測或整合測試，完整記錄跨層拖曳前後的 DOM 狀態差異
- [x] 完成一輪更完整的端到端整合驗證紀錄，串起同層拖放、跨層拖放、同步提示與權限流程
- [x] 釐清跨層拖曳回根層級後的排序狀態，確認先前重複 sortOrder 屬於請求進行中的暫時中間狀態，最終 snapshot 會穩定收斂為連續排序
- [x] 重新成功更新 todo.md 的整合驗證完成狀態，並以 integration-summary.md 與 integration-trace.md 補齊對應證據鏈
- [x] 補做一份可追溯的整合驗證紀錄，逐步列出登入、邀請接受、權限限制、Markdown、協作同步、同層拖放與跨層拖放的實際操作與結果
- [x] 將前端拖曳驗證項目明確落在新增可觀測驗證腳本與紀錄，而非未實作的正式瀏覽器整合測試
- [x] 建立名為 notion-like-collab 的 private GitHub repository，並將目前專案完整推送到遠端
- [x] 驗證 GitHub 遠端已建立且預設分支已包含最新專案內容
